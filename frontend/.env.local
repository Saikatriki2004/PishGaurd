# PhishGuard Frontend Environment Configuration

# API Base URL (Flask backend)
NEXT_PUBLIC_API_URL=http://127.0.0.1:5000

# Enable mock mode for development without backend
# Set to 'true' to use mock data instead of real API calls
NEXT_PUBLIC_USE_MOCKS=false

# Admin key for governance operations (must match Flask's PHISHGUARD_ADMIN_KEY)
NEXT_PUBLIC_ADMIN_KEY=phishguard-dev-admin-key-2026
